package com.plugin.repository;

import com.plugin.entity.StationManagerApplicationDocument;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StationManagerApplicationDocumentRepository extends JpaRepository<StationManagerApplicationDocument, Long> {
}
